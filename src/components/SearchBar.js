const SearchBar = () =>
{
    return(
        <div>
        <input type="search" placeholder="Where are you looking?"></input>
        <button>Find</button>
        </div>
    )
}

export default SearchBar